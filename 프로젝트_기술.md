# Frontend
Node.js + Next.js

# Backend
FastAPI (Python 3.11, Uvicorn ASGI)

# DB
PostgreSQL 16
- ORM: SQLAlchemy 2.0 (Async)
- 마이그레이션: Alembic

# 캐시
Redis 7 (세션, 캐싱, Rate Limiting)

# 실시간 채팅
- WebSocket (FastAPI 내장)
- Redis Pub/Sub (서버 간 메시지 브로드캐스트)

# 검색 엔진
Elasticsearch 8.x

# 인증/보안
- JWT (HS256) - Access/Refresh Token
- OAuth 2.0 (Google, Kakao, Naver)
- Bcrypt 비밀번호 해싱

# 파일 스토리지
Cloudflare R2 (S3 호환 오브젝트 스토리지)

# 백그라운드 작업
Celery + Redis (메시지 브로커)
- Flower (모니터링 대시보드)

# 모니터링/에러 트래킹
- Sentry (에러 추적) 채
- Prometheus (메트릭 수집)

# 인프라/배포
- Docker + Docker Compose
- Nginx (리버스 프록시, SSL)

# 테스트
- pytest + pytest-asyncio